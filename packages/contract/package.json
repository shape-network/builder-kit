{
  "name": "@builder-kit/contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile && bun run generate:artifact",
    "test": "hardhat test",
    "deploy": "hardhat ignition deploy ./ignition/modules/HelloShape.js",
    "deploy:shape-sepolia": "hardhat ignition deploy ./ignition/modules/HelloShape.js --network shapeSepolia && bun run generate:artifact",
    "deploy:shape-mainnet": "hardhat ignition deploy ./ignition/modules/HelloShape.js --network shapeMainnet && bun run generate:artifact",
    "generate:artifact": "node scripts/generate-deployment-artifact.js && (cd ../../apps/web && bun run wagmi:generate)",
    "verify": "hardhat run scripts/verify.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.19"
  }
}
